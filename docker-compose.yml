services:
  openclaw-agent:
    build: .
    container_name: openclaw-agent
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - ./models:/home/node/.cache/whisper
      - ./data:/home/node/data
